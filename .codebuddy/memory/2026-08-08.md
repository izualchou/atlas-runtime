# 2026-08-08

## Architecture Gap Analysis & Optimization (v9.0 → v9.1)

Completed comprehensive architecture-to-code gap analysis and implementation of all identified fixes:

### P0 (Critical) — Completed
- **P0-1: BaseExecutor adoption** — Added `execute()` method to SafeShellExecutor wrapping `run_command()` and returning `ExecutorResult`. Updated Scheduler to accept `BaseExecutor` protocol (calling `executor.execute(cmd, timeout)` instead of `executor(cmd, timeout)`). Updated Bootstrap to pass executor instance, not `run_command` method reference. Fixed integration tests and scheduler tests.
- **P0-2: transport cross-layer dependency** — Was already fixed (imports `BackpressureError` from `models.errors`).

### P1 (Consistency) — Completed
- **P1-1: Task/TaskStatus migration** — Created `models/task.py`, extracted from `core/scheduler.py`. Exported via `models/__init__.py`. Updated all import paths.
- **P1-2+P1-3: Import & docstring fixes** — Was already done (runtime/app.py uses `from device import`, detector.py docstrings are correct).
- **P1-4: New test coverage** — Created test_models.py (21 tests), test_device.py (12 tests), test_executor_base.py (7 tests), test_sim_switch.py (14 tests). All 54 tests pass.

### P2 (Tech Debt) — Completed
- **P2-1: Termux path dedup** — Fixed `ui_automation.py` to import from `device` instead of local `_TERMUX_PREFIX`/`_TERMUX_TMP`. shell_executor.py was already fixed.
- **P2-2: storage deprecation notice** — Added comments to `storage/__init__.py` marking `StorageFullError`/`StorageError` as deprecated export paths.

### Results
- 236/236 regression tests passing (12 known flaky deselected)
- 54 new tests, 0 new failures introduced
- ARCHITECTURE.md updated to v9.1
- MEMORY.md updated with new architecture facts
